% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conley_VCV.R
\name{conley_SE}
\alias{conley_SE}
\title{Conley Spatial HAC Variance-Covariance Estimation}
\usage{
conley_SE(
  reg,
  unit,
  time,
  lat,
  lon,
  kernel = "bartlett",
  dist_fn = "Haversine",
  dist_cutoff = 500,
  lag_cutoff = 5,
  lat_scale = 111,
  verbose = FALSE,
  cores = 1,
  balanced_pnl = FALSE
)
}
\arguments{
\item{reg}{A regression object of class \code{"felm"} from \code{\link[lfe:felm]{lfe::felm()}}.
Must be estimated with \code{keepCX = TRUE} and with latitude/longitude
variables passed as cluster variables.}

\item{unit}{Character string naming the cross-sectional unit identifier
variable in the \code{felm} cluster variables.}

\item{time}{Character string naming the time period identifier variable
in the \code{felm} cluster variables.}

\item{lat}{Character string naming the latitude variable in the \code{felm}
cluster variables.}

\item{lon}{Character string naming the longitude variable in the \code{felm}
cluster variables.}

\item{kernel}{Character string specifying the kernel function for spatial
weighting. One of \code{"bartlett"} (default), \code{"epanechnikov"}, \code{"gaussian"},
\code{"parzen"}, \code{"biweight"}, or \code{"uniform"}.}

\item{dist_fn}{Character string specifying the distance function.
\code{"Haversine"} (default) for great-circle distance in km, or \code{"SH"} for
the 111 km/degree approximation.}

\item{dist_cutoff}{Numeric. The spatial bandwidth (cutoff distance in km)
beyond which observations receive zero weight. Default is \code{500}.}

\item{lag_cutoff}{Numeric. The temporal bandwidth (number of time periods)
for serial correlation correction. Default is \code{5}.}

\item{lat_scale}{Numeric. Scaling factor for latitude (km per degree).
Default is \code{111}.}

\item{verbose}{Logical. If \code{TRUE}, prints progress messages during
computation. Default is \code{FALSE}.}

\item{cores}{Integer. Number of CPU cores for parallel computation via
\code{\link[parallel:mclapply]{parallel::mclapply()}}. Default is \code{1} (no parallelism).}

\item{balanced_pnl}{Logical. If \code{TRUE}, assumes a balanced panel and
pre-computes the distance matrix once for efficiency. Default is \code{FALSE}.}
}
\value{
A named list of three variance-covariance matrices, each of
dimension k x k where k is the number of regressors:
\describe{
\item{OLS}{The standard OLS variance-covariance matrix from the felm
object.}
\item{Spatial}{The spatial-only Conley VCV, correcting for
cross-sectional spatial correlation.}
\item{Spatial_HAC}{The full spatial HAC VCV, correcting for both
spatial and serial correlation.}
}
}
\description{
Computes Conley (1999) spatial HAC (Heteroskedasticity and Autocorrelation
Consistent) variance-covariance matrices for regression models estimated
with \code{\link[lfe:felm]{lfe::felm()}}. Supports cross-sectional spatial correlation,
serial (temporal) correlation, and the combined spatial HAC estimator.
Multiple kernel functions and distance metrics are available.
}
\examples{
\donttest{
data(US_counties_centroids)
if (requireNamespace("lfe", quietly = TRUE)) {
  reg <- lfe::felm(noise1 ~ noise2 | unit + year | 0 | lat + lon,
                    data = US_counties_centroids, keepCX = TRUE)
  vcvs <- conley_SE(reg, unit = "unit", time = "year",
                    lat = "lat", lon = "lon",
                    kernel = "bartlett", dist_cutoff = 500)
  # Spatial standard errors:
  sqrt(diag(vcvs$Spatial))
}
}
}
\references{
Christensen, D., Hartman, A. C. and Samii, C. (2021). Legibility and
external investment: An institutional natural experiment in Liberia.
\emph{International Organization}, 75(4), 1087--1108.
\doi{10.1017/S0020818321000187}

Conley, T. G. (1999). GMM estimation with cross sectional dependence.
\emph{Journal of Econometrics}, 92(1), 1--45.
\doi{10.1016/S0304-4076(98)00084-0}

Lehner, A. (2026). Bandwidth selection for spatial HAC standard errors.
\emph{arXiv preprint} arXiv:2603.03997. \doi{10.48550/arXiv.2603.03997}

Newey, W. K. and West, K. D. (1987). A simple, positive semi-definite,
heteroskedasticity and autocorrelation consistent covariance matrix.
\emph{Econometrica}, 55(3), 703--708. \doi{10.2307/1913610}
}
